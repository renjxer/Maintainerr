name: "Build Development"

on:
  push:
    branches:
      - "development"
      - "main"
  pull_request:
    branches:
      - "development"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  # 💡 直接手工在这里把末尾固定写死成小写的 maintainerr，避开变量中的大写 M
  GHCR_SLUG: ghcr.io/${{ github.repository_owner }}/maintainerr

jobs:
  guard_manual_run:
    uses: ./.github/workflows/guard-manual.yml
    with:
      enabled: ${{ github.event_name == 'workflow_dispatch' }}
      actor: ${{ github.actor }}
      source: manual workflow run

  build_docker_image:
    name: Build ${{ matrix.platform }}
    needs: guard_manual_run
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux/amd64
          - os: ubuntu-24.04-arm
            platform: linux/arm64
    runs-on: ${{ matrix.os }}
    steps:
      - name: Prepare
        if: github.event_name != 'pull_request'
        run: |
          platform=${{ matrix.platform }}
          echo "PLATFORM_PAIR=${platform//\//-}" >> $GITHUB_ENV

      - name: Check out the repo
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.GHCR_SLUG }}
          flavor: |
            lower=true

      - name: Log in to GitHub Container registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: >-
          ${{
            github.event_name == 'pull_request' &&
            'Build image' ||
            'Build & push by digest'
          }}
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: ${{ matrix.platform }}
          build-args: |
            GIT_SHA=${{ github.sha }}
            VERSION_TAG=development
          labels: ${{ steps.meta.outputs.labels }}
          outputs: >-
            type=image,"name=${{ env.GHCR_SLUG }}",
            push-by-digest=true,name-canonical=true,
            push=${{ github.event_name != 'pull_request' }}

      - name: Export digest
        if: github.event_name != 'pull_request'
        run: |
          mkdir -p ${{ runner.temp }}/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "${{ runner.temp }}/digests/${digest#sha256:}"

      - name: Upload digest
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v7
        with:
          name: digests-${{ env.PLATFORM_PAIR }}
          path: ${{ runner.temp }}/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    name: Merge Docker digests and push
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    needs:
      - build_docker_image
    steps:
      - name: Download digests
        uses: actions/download-artifact@v8
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-*
          merge-multiple: true

      - name: Log in to GitHub Container registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ${{ env.GHCR_SLUG }}
          tags: |
            type=raw,value=development
          labels: org.opencontainers.image.revision=${{ github.sha }}
          flavor: |
            lower=true

      - name: Create manifest list and push
        working-directory: ${{ runner.temp }}/digests
        run: |
          echo "正在生成多架构开发版标签..."
          
          # 直接指定标签为 development，简单粗暴，绝对不会因为找不到变量而报错
          docker buildx imagetools create \
            --tag "${{ env.GHCR_SLUG }}:development" \
            $(printf '${{ env.GHCR_SLUG }}@sha256:%s ' *)
